## Apps Script backup

The file `apps-script/hnavi-scraper.js` is a backup copy of the Google Apps Script code.

It must be copied into Google Apps Script manually or via clasp. It will not run inside Cloudflare Workers because it depends on Google Apps Script services such as SpreadsheetApp, PropertiesService, ScriptApp, and UrlFetchApp.